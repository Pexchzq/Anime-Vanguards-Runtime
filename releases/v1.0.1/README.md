# Anime Vanguards Macro System v1.0.1

Fixes the `MAP-MACRO-CONFIG.lua` compile error caused by nested Lua long
brackets inside its header comment.

```lua
loadstring(game:HttpGet("https://raw.githubusercontent.com/Pexchzq/Anime-Vanguards-Runtime/main/releases/v1.0.1/BOOTSTRAP.lua"))()
```

Stop Brain, Reader, and Controller:

```lua
_G.AVStop()
```
